function arrayBuilders(one, two, three){
    var arr = [];
    arr.push('one');
    arr.push('two');
    arr.push('three');
    arr.pop();
    console.log(arr)
    return arr
}
arrayBuilders()

console.log(Math.pow(2,3))
